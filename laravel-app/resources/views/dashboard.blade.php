@extends('layouts.app') @section('content')<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h2">Dashboard</h1><p class="text-secondary mb-0">Welcome, {{ auth()->user()->name }}.</p></div><a href="{{ route('analyze') }}" class="btn btn-success">Analyze Food</a></div><div class="row g-3 mb-4"><div class="col-md-6"><div class="card"><div class="card-body"><p class="text-secondary mb-1">Today's Calories</p><div class="display-6 fw-semibold">{{ number_format($todayCalories, 0) }} kcal</div></div></div></div><div class="col-md-6"><div class="card"><div class="card-body"><p class="text-secondary mb-1">Foods Analyzed</p><div class="display-6 fw-semibold">{{ $foodCount }}</div></div></div></div></div><div class="card"><div class="card-body"><div class="d-flex justify-content-between"><h2 class="h5">Recent Analysis</h2><a href="{{ route('history') }}">View all</a></div>@forelse($histories as $history)<div class="border-top py-3 d-flex justify-content-between"><span>{{ $history->food->food_name }}<small class="d-block text-secondary">{{ number_format($history->portion_gram, 0) }}g</small></span><strong>{{ number_format($history->total_calories, 0) }} kcal</strong></div>@empty<p class="text-secondary mb-0">No analysis records yet.</p>@endforelse</div></div>@endsection